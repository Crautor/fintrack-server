datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model FinancialGoal {
  financialGoalId Int      @id @default(autoincrement())
  userId          Int
  value           Float
  limitDate       DateTime
  status          String
}
